(ns
    ^{:doc "Unit tests."
      :author "Paul Evans"}
  googlecodejam.alienlanguage.test.core
  (:use [googlecodejam.alienlanguage.core])
  (:use [clojure.test]))

(deftest
  ^{:doc "Tests for first-symbol"}
  test-first-symbol)